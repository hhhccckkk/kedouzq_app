package com.hck.zhuanqian.ui;import android.os.Bundle;import android.os.Handler;import android.os.Message;import android.view.View;import android.webkit.WebView;import cn.yeeguo.Yeeguo;import cn.yeeguo.YeeguoScoreOnListener;import com.hck.kedouzq.R;import com.hck.zhuanqian.data.Contans;/** *  * @author *  */public class YeGuoActivity extends BaseActivity implements YeeguoScoreOnListener {    private int point;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        initYeGuo();        setContentView(R.layout.activity_yeguo);        initTitle("小椰专区");        getAdInitData();        initDownSize(findViewById(R.id.down_size));        webView = (WebView) findViewById(R.id.dianle_webview);        initDate();    }    private void initDate() {        initTitle("小椰赚钱区");        webView.loadUrl("file:///android_asset/yeguo.html");    }    private void initYeGuo() {        try {            Yeeguo.initYeeguo(this, Contans.YE_GUO, null);        } catch (Exception e) {            showErrorToast();        }    }    public void startGetMoney(View view) {        showYG();    }    private void showYG() { // 展示点乐积分墙        Yeeguo.showOffers(this, this);    }    @Override    public void yeeguoErrorListener(String arg0) {    }    @Override    public void yeeguoScoreListener(int arg0, int arg1, int arg2, String arg3, String arg4, String arg5) {        if (arg2 > 0) {            Message message = new Message();            message.what = arg2;            handler.sendMessage(message);        } else {        }    }    Handler handler = new Handler() {        public void handleMessage(android.os.Message msg) {            point = msg.what;            savePoint(Contans.AD_NAME_YEGUO, point);        };    };    @Override    protected void onResume() {        super.onResume();    }}